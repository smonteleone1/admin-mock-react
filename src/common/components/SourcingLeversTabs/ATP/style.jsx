import styled from 'styled-components';

export const ATPDiv = styled.div(() => `
    .cost {
        display: flex;
        margin: auto;
        float: left;
    },
    .nodeCost {
        margin-right: 48px;
    },
    .p {
        margin: 8px;
        margin-bottom: 20px;
    }
`);

export default ATPDiv;
